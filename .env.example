APP_ENV=development
DATABASE_URL=postgres://fakturomat:fakturomat@localhost:55433/fakturomat
DATABASE_MAX_CONNECTIONS=10
# Optional migration-only URL; use a DIRECT connection for Neon (no -pooler).
# For postgres.js, use sslmode=verify-full without libpq's channel_binding option.
# DATABASE_MIGRATION_URL=postgresql://USER:PASSWORD@DIRECT_HOST/DATABASE?sslmode=verify-full
# Public origin of this deployment (without a trailing path).
BETTER_AUTH_URL=http://localhost:5173
# Generate once with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-with-a-random-base64-secret
# Logos and expense attachments keep their existing local object storage.
STORAGE_LOCAL_ROOT=./data/storage
# Invoice PDFs: local when S3_BUCKET is empty/unset, otherwise private AWS S3.
LOCAL_STORAGE_PATH=./data/documents
# S3_BUCKET=my-private-invoice-bucket
# S3_REGION=eu-central-1
# AWS_REGION=eu-central-1
# AWS SDK resolves credentials itself (profile, IAM role, Cloud Connection, etc.).
# Generate once with: openssl rand -base64 32
BANK_CREDENTIALS_ENCRYPTION_KEY=replace-with-base64-encoded-32-byte-key

# Used only by `deno task user:create`; remove the password after bootstrap.
FAKTUROMAT_ADMIN_EMAIL=admin@example.test
FAKTUROMAT_ADMIN_NAME=Admin
FAKTUROMAT_ADMIN_PASSWORD=replace-with-at-least-12-characters
